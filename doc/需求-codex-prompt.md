## 需求规范

```text
请根据以上规范生成以下所有文件的完整代码。确保代码可直接运行，包含 TypeScript 类型，Tailwind 类名正确，并实现所有动态效果（滚动动画、暗色模式可选）。如果使用 framer-motion，请在文件中导入并配置。所有样式尽量用 Tailwind 实现，必要时可添加自定义 CSS。

文件清单：

app/layout.tsx

app/page.tsx

app/blog/page.tsx

app/blog/[slug]/page.tsx

app/about/page.tsx

app/privacy/page.tsx

app/sitemap.ts

app/robots.ts

components/Header.tsx

components/Footer.tsx

components/Newsletter.tsx

components/PostCard.tsx

lib/posts.ts

types/post.ts

posts/hello-office-yoga.md

tailwind.config.js（如需自定义）

styles/globals.css（如需额外样式）

确保所有文件路径正确，依赖已安装。不需要解释，直接输出代码块，并标注文件名。

```


